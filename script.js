function functionName() {
  // Get the function name from user input
  var userInput = prompt("Enter the function name:");

  // Display the entered function name using an alert statement
  alert(userInput);
}

// Example usage
functionName(); // This will prompt the user to enter a function name and display it in an alert
